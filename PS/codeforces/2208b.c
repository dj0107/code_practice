#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
typedef struct card {
		int cost;
		struct card *next;
	} card;
int main() {
	int t;
	scanf("%d", &t);
	for (int i=0; i<t; i++) {
		int n,k,p,m;
		scanf("%d%d%d%d", &n, &k, &p, &m);
		// int price[5001];
		// for (int j=1; j<=n; j++) scanf("%d", &price[j]);
		card top;

		for (int j=0; j<n; j++) {
			
		}

		bool finish = false;



	}
}